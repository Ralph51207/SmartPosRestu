name: smart_restupos
description: "SmartServe POS - AI-driven restaurant point-of-sale system"
publish_to: 'none'
version: 0.1.0

environment:
  sdk: ^3.9.2

dependencies:
  flutter:
    sdk: flutter
  
  # Firebase
  firebase_core: ^3.8.1
  firebase_auth: ^5.3.3
  firebase_database: ^11.3.3
  cloud_firestore: ^5.6.0
  
  # State Management
  provider: ^6.1.2
  
  # HTTP & API
  http: ^1.2.2
  
  # Charts & Visualization
  fl_chart: ^0.69.2
  
  # UI Components
  intl: ^0.19.0
  google_fonts: ^5.0.0
  
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/LOGO.png
